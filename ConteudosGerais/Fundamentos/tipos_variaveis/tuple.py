# a variável tupla é semelhante a lista, mas com uma diferença fundamental:
# as tuplas são IMUTÁVEIS. Se vc cria uma tupla, não pode modificar seus elementos, 
# adicionar ou remover os existentes

tupla_imutavel = (1, 2, 3, 'abacaxi', 5) # criando a variável tuple e atribuindo o valor '(1, 2, 3, 'abacabi', 5)'
print(tupla_imutavel) # imprimindo o valor da variável